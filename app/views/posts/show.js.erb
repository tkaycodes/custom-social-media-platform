$(document).ready(function(){
  $("#modal-window").html("<%= escape_javascript(render 'posts/form') %>");

  
  $('#likebutton').on("click", function(){
    console.log("liked");
    $.ajax({
      url: 'likes',
      method: 'post',
      dataType: 'json',
      data: {id: '<%=@post.id%>'},
      success: function(data)
      {
           console.log(data);
      },
      error: function(result){
        console.log(result);
      }
      // error: function(result){
      //     console.log(result);
      // };

    });
  });

  if ($('#unlikebutton').length) {
    $('#unlikebutton').on("click", function(){

   console.log('hi');

  });
  }
  

});
